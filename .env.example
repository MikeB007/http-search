# HTTP Search Application Configuration

# SSL Certificate Settings
PFX_PATH=./certs/production.p12
SSL_PASSPHRASE=production123

# Server Configuration  
NODE_ENV=development
HTTP_PORT=8080
HTTPS_PORT=8443

# Certificate Hosts (automatically detected)
PUBLIC_IP=
INTERNAL_IP=

# Valid certificate hosts: localhost, 127.0.0.1, base
